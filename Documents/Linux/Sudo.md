## Notes

- For permissions denied etc, instead of changing /etc/sudoers file, add file in sudoers.d/ directory.
- Only users who are listed in the **/etc/sudoers** file or the **/etc/sudoers.d** directory have permission to use the 'sudo -i' command to switch to the root user.